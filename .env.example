# Copy this file to .env and fill in your key(s)
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
# When running locally without Docker you can use ./app.db
# In Docker we default to /data/app.db via compose
DB_PATH=./app.db
